# PostgreSQL
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=review_db
POSTGRES_USER=review_user
POSTGRES_PASSWORD=change_me_in_prod

# Constructed automatically in config.py from the above — override if needed
# DATABASE_URL=postgresql+asyncpg://review_user:change_me_in_prod@postgres:5432/review_db

# Temporal
TEMPORAL_HOST=temporal
TEMPORAL_PORT=7233
TEMPORAL_NAMESPACE=default
TEMPORAL_TASK_QUEUE=review-task-queue

# App
APP_ENV=development
APP_PORT=8000
LOG_LEVEL=INFO

# AI Summary
AI_SUMMARY_MOCK=true
OPENAI_API_KEY=sk-replace-me
